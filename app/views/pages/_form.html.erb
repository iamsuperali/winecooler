<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
        <% @page.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title,"标题" %>:
    <%= f.text_field :title,:style=>"width:500px;" %>
  </div>

  <div class="field">
    <%= f.label :subtitle,"副标题" %>:
    <%= f.text_field :subtitle,:style=>"width:500px;" %>
  </div>

  <div class="field">
    <%= f.label :content,"内容" %>:
    <%= f.cktext_area :content,
      :toolbar => 'Full',
      :width => 800,
      :height => 200,
      :language => 'zh-cn',
      :filebrowserBrowseUrl => "/ckeditor/attachment_files",
      :filebrowserUploadUrl => "/ckeditor/attachment_files",
      :filebrowserImageBrowseUrl => "/ckeditor/pictures",
      :filebrowserImageUploadUrl => "/ckeditor/pictures"
  %>
  </div>
  <!--div class="field">
  <%= f.label :status %><br />
  <%= f.number_field :status %>
  </div-->
  <!--div class="field">
  <%= f.label :permalink %><br />
  <%= f.text_field :permalink %>
  </div-->
  <div class="actions">
    <%= f.submit "确定" %>
  </div>
<% end %>
